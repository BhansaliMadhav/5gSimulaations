/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: false, // This ensures dark mode is turned off
  theme: {
    extend: {
      colors: {
        primary: "#1f2937", // Customize the colors
        secondary: "#f3f4f6",
      },
    },
  },
  plugins: [],
};
